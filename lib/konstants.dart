import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF5C6BC0);
const kSecondaryColor = Color(0xFF1DE986);
const KbgColor = Colors.white;

const kGradient = LinearGradient(
    colors: [Color(0xFFE8EAF6), Color(0xFF7986CB)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

const kDuration = Duration(milliseconds: 200);

const kDefaultShadow =
    BoxShadow(offset: Offset(0, 10), blurRadius: 10, color: Colors.black12);
